# ---------------------------------------------------------------------------
# 자격 증명 — 절대 커밋하지 말 것
# APP_SECRET_KEY 는 세션 쿠키를 복호화할 수 있는 키다
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example

# ---------------------------------------------------------------------------
# 수집 데이터, 백업, 로그
# 백업 파일에는 독자 닉네임과 댓글 본문이, DB에는 암호화된 세션 쿠키가 들어 있다
# data/ 와 logs/ 는 코드가 자동으로 만들므로 .gitkeep 이 필요 없다
# ---------------------------------------------------------------------------
data/
logs/
*.db
*.sqlite3
*.sqlite3-journal

# ---------------------------------------------------------------------------
# 사용자별 설정
# targets.yaml 은 내 블로그 글 목록이라 개인 것이다. 예시 파일만 커밋한다.
# 파일이 없어도 빈 목록으로 정상 기동한다 (app/config/targets.py)
#
# config/rules.yaml 은 무시하지 않는다. whitelist 가 비어 있어 개인정보가 없고,
# 스팸 규칙 템플릿 역할을 하기 때문이다.
# ---------------------------------------------------------------------------
config/targets.yaml

# ---------------------------------------------------------------------------
# 파이썬
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.so
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/
*.egg-info/

# ---------------------------------------------------------------------------
# 편집기와 OS
# ---------------------------------------------------------------------------
.DS_Store
._*
Thumbs.db
.idea/
.vscode/
*.swp

ref/
.claude/
docs/
tests/